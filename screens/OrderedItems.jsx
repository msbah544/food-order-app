import React from "react";
import { View } from "react-native";
import { Text } from "react-native-paper";

const OrderedItems = ({ navigation }) => {
  return (
    <View>
      <Text>Ordered food items</Text>
    </View>
  );
};

export default OrderedItems;
